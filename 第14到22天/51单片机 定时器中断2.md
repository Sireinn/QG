## 定时器配置
- 对TMOD赋值 以确定T0T1的工作方式
- 根据所要定时的时间计算初值，并将其写入TH0，TL0或TH1/TL1
- 如果使用中断，则对EA赋值开放定时器中断
- 使TR0TR1置位 启动定时计数器定时或计数

		 void time0_init(void)
	{
		TMOD|=0X01;
		TH0=0XFC;
		TL0=0X18;
		ET0=1;
		EA=1
		TR0=1;
	}

- TMOD|=0X01;//定时器设置为T0 工作方式1  |或 不干扰其他工作
![[Pasted image 20260305165247.png]]
- TH0=0XFC;//给定时器赋初值定时1ms
- TL0=0X18; 
- ET0=1;打开中断允许位
- EA=1；打开总中断
- TR0=1打开定时器


## 硬件设计
验证定时器工作用到D1连接到P2.0管脚
定时器属于内部功能

## 软件设计
实现功能 通过定时器0终端控制D1指示灯间隔1s闪烁
static u16 i=0; **静态变量** 自动保存上一次的值不会被赋为0；
	typedef unsigned int u16;
	typedef unsigned char u8;
	
	void time0_init(void)
	{
		TMOD|=0X01;
		TH0=0XFC;
		TL0=0X18;
		ET0=1;
		EA=1;
		TR0=1;
	}
	
	void delay_10us(u8 ten_us)
	{
		while(ten_us--);
	}
	
	sbit LED1=P2^0;
	
	
	
	void main()
	{
		time0_init();
		while(1)
		{
			
		}
		
	}
	void time0() interrupt 1
	{
		static u16 i=0;
		
		TH0=0XFC;
		TL0=0X18;
		i++;
		if(i==1000)
		{ 
			i=0;
			LED1=!LED1;
		}
	}



